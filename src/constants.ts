export const FAVORITE_SHOWS = "favoriteShows";

/** Horizontal gutter applied by the screens; the year strip cancels it to go full-bleed. */
export const SCREEN_PADDING = 12;

/** `selectedYear` sentinels for the two non-year tabs. */
export const FAVORITES_TAB = 0;
export const TODAY_TAB = -1;
